name: Beta Release

on:
  push:
    branches:
      - beta

permissions:
  contents: write

jobs:
  build-and-beta-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Build compendium packs
        run: npm run build:packs

      - name: Create ZIP
        run: |
          mkdir -p staging/t20-theme-overhaul
          cp module.json           staging/t20-theme-overhaul/module.json
          cp dist/main.bundle.js   staging/t20-theme-overhaul/main.bundle.js
          if [ -d dist/assets ]; then
            cp -r dist/assets staging/t20-theme-overhaul/assets
          fi
          if [ -d packs ]; then
            cp -r packs staging/t20-theme-overhaul/packs
          fi
          cd staging
          zip -r ../t20-theme-overhaul-beta.zip t20-theme-overhaul/
          cd ..
          echo "ZIP contents:"
          unzip -l t20-theme-overhaul-beta.zip

      - name: Delete existing beta-latest release and tag
        run: |
          gh release delete beta-latest --yes 2>/dev/null || true
          git push origin --delete beta-latest 2>/dev/null || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create beta pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: beta-latest
          name: "Beta (latest)"
          body: |
            ## Beta Build

            Versão de teste automática gerada pelo push na branch `beta`.

            **⚠️ Não use em sessões de jogo — apenas para testes.**

            ### Como instalar no Foundry VTT

            Foundry → **Módulos** → **Instalar Módulo** → cole a URL do manifesto:

            ```
            https://raw.githubusercontent.com/${{ github.repository }}/beta/module.json
            ```

            ### Como atualizar

            Se você já tem o módulo beta instalado, basta clicar em **Verificar Atualizações**
            no Foundry — ele baixará esta build automaticamente.
          prerelease: true
          files: t20-theme-overhaul-beta.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
