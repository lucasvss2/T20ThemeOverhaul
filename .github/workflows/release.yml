name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Version tag (e.g. v1.0.0)"
        required: true

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    # Run JS-based actions on Node 24 (Node 20 is deprecated on GitHub runners).
    # Covers softprops/action-gh-release@v2 regardless of its pinned runtime.
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Resolve version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi
          # Strip leading "v"
          VERSION="${TAG#v}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Patch version and download URL in module.json
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          TAG="${{ steps.version.outputs.tag }}"
          jq \
            --arg v "$VERSION" \
            --arg dl "https://github.com/${{ github.repository }}/releases/download/$TAG/aeris-bg3-rolls-t20.zip" \
            '.version = $v | .download = $dl' \
            module.json > module.patched.json
          mv module.patched.json module.json

      - name: Build compendium packs
        run: npm run build:packs

      - name: Create ZIP
        run: |
          mkdir -p staging/aeris-bg3-rolls-t20
          cp module.json           staging/aeris-bg3-rolls-t20/module.json
          cp dist/main.bundle.js   staging/aeris-bg3-rolls-t20/main.bundle.js
          # Assets (PNGs, ícones, etc.) — vite.config.ts copia public/ pra dist/
          if [ -d dist/assets ]; then
            cp -r dist/assets staging/aeris-bg3-rolls-t20/assets
          fi
          # Compêndios do módulo (LevelDB compilado de packs-src/ pelo build:packs)
          if [ -d packs ]; then
            cp -r packs staging/aeris-bg3-rolls-t20/packs
          fi
          cd staging
          zip -r ../aeris-bg3-rolls-t20.zip aeris-bg3-rolls-t20/
          cd ..
          echo "ZIP contents:"
          unzip -l aeris-bg3-rolls-t20.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          generate_release_notes: true
          files: aeris-bg3-rolls-t20.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
