refactor: unify logging via log/warn/error helper (v1.25.3)

Phase 3 of the code-cleanliness plan. Replaces 85 raw console.log/warn/error
calls across 15 files with the existing log/warn/error helpers from
@/utils/logging, stripping the per-call hardcoded prefixes
([t20-theme-overhaul] and [${MODULE_ID}]/[${cfg.moduleId}]) so every log line
now carries a single, consistent prefix from one place.

- Imports normalized per file (only the helpers actually used; bare-call
  detection so ui.notifications.warn/error don't trigger spurious imports).
- Dropped now-unused MODULE_ID imports in the t20-fixes (only used to build
  the old log prefix).
- global.d.ts and tests untouched; logging.ts keeps the sole console.* sinks.

Behaviour-neutral (log text/prefix only). typecheck + lint clean, 216 tests
pass, and an in-engine smoke test (module load + Aura Sagrada cast) confirms
no runtime regression. Test resources restored to baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
